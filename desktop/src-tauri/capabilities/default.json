{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop shell: the About webview invokes no Tauri commands; the Settings webview invokes the app-defined read_settings/write_setting commands (app commands aren't ACL-gated — they shell out to the bundled CLI from Rust, which bypasses the capability layer). The sidecar and browser are driven from Rust.",
  "windows": ["about", "settings"],
  "permissions": ["core:default"]
}
