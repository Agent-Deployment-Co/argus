{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop shell: the static About webview invokes no Tauri commands. The sidecar and browser are driven from Rust, which does not go through the capability/permission layer.",
  "windows": ["about"],
  "permissions": ["core:default"]
}
