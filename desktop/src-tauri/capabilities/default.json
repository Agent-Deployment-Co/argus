{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop shell: the About webview invokes no Tauri commands. The sidecar and browser are driven from Rust.",
  "windows": ["about"],
  "permissions": ["core:default"]
}
