node_modules/
dist/
docs/.vitepress/cache/
docs/.vitepress/dist/
data/
/tmp/
*.html
*.db
!web/index.html
*.report.json
argus-report.*
.DS_Store
*updater*.key
*updater*.key.pub
desktop/src-tauri/target/
desktop/src-tauri/binaries/
desktop/src-tauri/gen/
desktop/src-tauri/web/
*.bun-build
.obsidian

# Tauri build artifacts and staged sidecar/web (regenerated from dist/ by the build)
desktop/src-tauri/target/
desktop/src-tauri/binaries/
desktop/src-tauri/web/
desktop/src-tauri/gen/
# Keep the tray app's bundled UI files (its frontendDist); the *.html rule above would hide them.
!desktop/ui/index.html
!desktop/ui/about.html
!desktop/ui/settings.html

# vitepress build artifacts
docs/.vitepress/dist/
docs/.vitepress/cache/

# local env (docs analytics token, etc.) -- keep the checked-in example
.env
.env.*
!.env.example

# demo sandbox (generated by scripts/demo.ts)
.demo/

# per-machine Tauri config overrides (e.g. signing identity), merged via `tauri build -c`
desktop/src-tauri/tauri.conf.json.local

# wrangler's local Miniflare state (`wrangler dev --local`) — #281's demo Worker
.wrangler/
.dev.vars
